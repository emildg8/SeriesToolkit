# Журнал изменений Script_Rename_ALLVideo

Формат: версия, дата и время (локально), кратко что сделано. Записи через `Publish-ToolkitRelease.ps1` получают метку времени автоматически. **Сначала — последний релиз.**

Нумерация релизов: **0.1.0** … **0.1.9**, затем **0.2.0** … **0.2.9**, далее **0.3.0** и т.д. (мажор · минор · патч).

## 0.4.8 — 2026-04-21 22:44:32

- **`Get-EpisodeNumbersFromVideoFilename`:** тег **«сезон×эпизод»** (`1x01` … `1x24`, `12x24`, диапазон `1x01-02`); отсев совпадений с разрешением (480, 720, 1080 и т.д.).
- **`Get-FallbackEpisodeTitleFromFilename`:** хвост после `1x01` при разделителях `._-`.
- **Шаблон CSV** и **диагностика:** упоминание формата **1x01**.
- **Снимок предыдущего релиза:** `Old/Script_Rename_ALLVideo_0.4.7.ps1`.
- `**Переименовать-сериал.cmd`** вызывает `Script_Rename_ALLVideo_0.4.8.ps1`.

## 0.4.7 — 2026-04-21 22:29:45

- **`Get-EpisodeNumbersFromVideoFilename`:** распознаёт тег **`.S01E.01.`** (точка между **E** и номером эпизода), в т.ч. диапазоны **S01E.01-E02**; **`Get-FallbackEpisodeTitleFromFilename`** — хвост после такого тега.
- **Шаблон `episode-titles.csv`:** в первой строке-подсказке перечислены допустимые форматы тега в имени файла.
- **Диагностика:** учёт `.S01E.01.` при проверке наличия тегов в именах.
- **Снимок предыдущего релиза:** `Old/Script_Rename_ALLVideo_0.4.6.ps1`.
- `**Переименовать-сериал.cmd`** вызывает `Script_Rename_ALLVideo_0.4.7.ps1`.

## 0.4.6 — 2026-04-21 22:24:05

- **Папки сезона (англ.):** если подпапки называются целиком **Season_01**, **Season_08**, **Season 1** (подчёркивание или пробел) — переименуются в **Сезон 1** … **Сезон N** (в т.ч. раскладка «Игра престолов»). `Resolve-SeasonNumberFromFolderName` учитывает тот же шаблон, чтобы обрабатывать файлы до и после переименования папки.
- **Снимок предыдущего релиза:** `Old/Script_Rename_ALLVideo_0.4.5.ps1`.
- `**Переименовать-сериал.cmd`** вызывает `Script_Rename_ALLVideo_0.4.6.ps1`.

## 0.4.5 — 2026-04-21 21:54:04

- **Регрессия 0.4.4:** подписи Кинопоиска **«Эпизод #1.1»** снова **не** считаются общим placeholder (`Test-EpisodeTitleLooksLikePlaceholder`) — иначе обнулялись заголовки в именах файлов и `Expand-EpisodeListWithRussianWikipedia` подменял список данными из Википедии (чужой сериал).
- **`Test-EpisodeTitleLooksLikeKinopoiskEpisodeLabel` / `Test-EpisodeListLooksLikeKinopoiskEpisodeLabels`:** явное распознавание таких подписей; в `Get-EpisodeListQualityScore` они снижают «оценку» списка; **`Expand-EpisodeListWithRussianWikipedia`** не трогает список, если он в основном из таких подписей.
- **Снимок предыдущего релиза:** `Old/Script_Rename_ALLVideo_0.4.4.ps1`.
- `**Переименовать-сериал.cmd`** вызывает `Script_Rename_ALLVideo_0.4.5.ps1`.

## 0.4.4 — 2026-04-21 21:48:23

- **Кинопоиск (сохранённый HTML):** `Get-EpisodesFromKinopoiskSavedHtmlFile` — для страниц с `kinopoisk.ru/film|series/<id>` не подмешивается парсинг викифрагмента (`Get-EpisodesFromWikipediaHtmlFragment`), чтобы случайная «wikitable» в разметке не подменяла список эпизодов чужим сериалом.
- **Замечание:** попытка отнести **«Эпизод #1.1»** к `Test-EpisodeTitleLooksLikePlaceholder` дала регрессию (подмена Википедией, пустые заголовки в именах файлов) — исправлено в **0.4.5** отдельными функциями `Test-EpisodeTitleLooksLikeKinopoiskEpisodeLabel` / `Test-EpisodeListLooksLikeKinopoiskEpisodeLabels`.
- **Снимок предыдущего релиза:** `Old/Script_Rename_ALLVideo_0.4.3.ps1`.
- `**Переименовать-сериал.cmd`** вызывает `Script_Rename_ALLVideo_0.4.4.ps1`.

## 0.4.3 — 2026-04-21 21:15:34

- **Парсинг Википедии (`Fetch-VideoMetadata.ps1`):** `Test-WikipediaEpisodeTitleCellPlausible` — учитываются **чисто цифровые** названия эпизодов (например **«1969»** у *Звёздных врат SG-1*, S02E21); по-прежнему отсекаются числа с разделителями тысяч и слишком короткие «цифровые» строки.
- **Правило Cursor:** `.cursor/rules/episode-titles-numeric.mdc` — напоминание агенту не ломать эту эвристику при правках.
- **Снимок предыдущего релиза:** `Old/Script_Rename_ALLVideo_0.4.2.ps1`.
- `**Переименовать-сериал.cmd`** вызывает `Script_Rename_ALLVideo_0.4.3.ps1`.

## 0.4.2 — 2026-04-21 20:15:16

- **Show-BilingualDialog** (сообщения с одной кнопкой OK и аналоги): `Set-AppleReadOnlyTextStyle` — фон как у формы (248,248,250), без рамки вокруг текста; отступ от текста до кнопки 12px.
- **Длинные пути:** `Format-UiPathForDisplay` — разбиение по `\` для длинных UNC/локальных путей в сообщениях «Отладка» и «Сохранено».
- **Снимок предыдущего релиза:** `Old/Script_Rename_ALLVideo_0.4.1.ps1`.
- `**Переименовать-сериал.cmd`** вызывает `Script_Rename_ALLVideo_0.4.2.ps1`.

## 0.4.1 — 2026-04-21 20:11:06

- **Многострочный текст:** `Normalize-UiMultilineText`, доработка `Measure-UiTextHeight` (подсчёт по строкам) — корректные абзацы и переносы в диалогах (список эпизодов, поиск Wikipedia, `Show-BilingualDialog` / «Сохранено», ручной режим, итог «Готово»).
- **Диалог списка эпизодов:** выпадающий список языка по ширине до правого края, вровень с кнопками «Обзор» и «Отмена».
- **Снимок предыдущего релиза:** `Old/Script_Rename_ALLVideo_0.4.0.ps1`.
- `**Переименовать-сериал.cmd`** вызывает `Script_Rename_ALLVideo_0.4.1.ps1`.

## 0.4.0 — 2026-04-21 20:03:30

- **Интерфейс (i18n):** первый запуск — английский; язык сохраняется в `%LOCALAPPDATA%\Script_Rename_ALLVideo\ui-language.txt`. Файл `UiStrings.ps1` (UTF-8 с BOM) — строки для en, ru, de, es, fr, zh-CN, ja, pt-BR, it, pl, uk, ko (fallback на en).
- **Диалог источника эпизодов:** выпадающий список языка, компактная вёрстка без лишнего скролла в подсказке.
- **Итоговый диалог (`Show-RenameOutcomeDialog`):** высота по содержимому, чекбокс справа от текста, без пустого низа.
- **Прочие диалоги:** кнопки в `Show-BilingualDialog` локализованы по выбранному языку; при длинном тексте окно подгоняется, по возможности без вертикального скролла (очень длинный текст — до ~520px с вертикальным скроллом).
- **Снимок предыдущего релиза:** `Old/Script_Rename_ALLVideo_0.3.2.ps1`.
- `**Переименовать-сериал.cmd`** вызывает `Script_Rename_ALLVideo_0.4.0.ps1`.

## 0.3.2 — 2026-04-21 19:33:21

- **Итоговый диалог:** исправлен шрифт заголовка — `System.Drawing.FontStyle` не содержит `Semibold` (из-за `[FontStyle]::Semibold` получался `$null` и падал `New-UiFont`). Используется гарнитура **Segoe UI Semibold** со стилем `Regular`.
- **Снимок предыдущего релиза:** `Old/Script_Rename_ALLVideo_0.3.1.ps1`.
- `**Переименовать-сериал.cmd`** вызывает `Script_Rename_ALLVideo_0.3.2.ps1`.

## 0.3.1 — 2026-04-21 19:30:35

- **Итоговый диалог (`Show-RenameOutcomeDialog`) и стиль UI:** исправлено создание `System.Drawing.Font` — `New-Object` выбирал неверную перегрузку `Font(string, float, GraphicsUnit)` вместо `Font(string, float, FontStyle)` (конфликт enum). Добавлена `New-UiFont`; все диалоги используют явный `[System.Drawing.Font]::new(...)`.
- **Снимок предыдущего релиза:** `Old/Script_Rename_ALLVideo_0.3.0.ps1`.
- `**Переименовать-сериал.cmd`** вызывает `Script_Rename_ALLVideo_0.3.1.ps1`.

## 0.3.0 — 2026-04-21 19:21:49

- **Авто-подстановка HTML из «Загрузки»:** при отсутствии `episode-titles.csv` скрипт ищет в папке загрузок (профиль пользователя) файлы `.html`/`.htm`, изменённые за последние 7 дней, и выбирает лучшее совпадение с именем папки сериала (нормализация: регистр, `ё`→`е`, `_`/тире→пробел, пунктуация; оценка по подстрокам и «словам»). Подставляется в поле диалога источника списка эпизодов; если подходящего файла нет — поле пустое. При переданном `-SourceUrl` поиск в «Загрузках» не выполняется.
- **Снимок предыдущего релиза:** `Old/Script_Rename_ALLVideo_0.2.9.ps1`.
- `**Переименовать-сериал.cmd`** вызывает `Script_Rename_ALLVideo_0.3.0.ps1`.

## 0.2.9 — 2026-04-21 20:15:00

- **Script_Rename_ALLVideo_0.2.9.ps1 — итоговое окно «Готово» (`Show-RenameOutcomeDialog`):** исправлена раскладка, когда до показа формы `ClientSize.Width` был 0 и кнопки «Да / Нет / Отмена» уезжали за левый край (окно казалось пустым). Добавлены оценка ширины по `Form.Width`, переразметка в `Load`/`Shown`, при необходимости расширение окна; форма поверх остальных (`TopMost`). Ошибки показа диалога пишутся в `rename-series-log.txt` (`OUTCOME_DIALOG_ERROR`) и дублируются в консоль.
- **Чекбокс очистки** в том же окне: `AutoSize = $false`, привязка колонки кнопок к правому краю подписи (`lbl.Right`).
- **Сообщение после сохранения `episode-titles.csv`:** структурированный текст RU/EN, отдельные строки для пути CSV, заголовок окна `Saved / Сохранено`.
- **Снимок предыдущего релиза:** `Old/Script_Rename_ALLVideo_0.2.8.ps1`.
- `**Переименовать-сериал.cmd`** вызывает `Script_Rename_ALLVideo_0.2.9.ps1`.

## 0.2.8 — 2026-04-21 17:35:00

- **Script_Rename_ALLVideo_0.2.8.ps1:** добавлены защитные остановки от «пустого»/placeholder-переименования. Если полученный список эпизодов содержит заглушки вида «Эпизод N/Серия N» или после нормализации не остаётся ни одного валидного названия, скрипт завершает работу с предупреждением вместо массового переименования в `SxxEyy` без названий.
- **Автообновление CSV:** при найденном локальном `episode-titles.csv` с плейсхолдерами скрипт пытается тихо пересобрать список из `SourceUrl`/поиска; при неудаче останавливается и просит корректный источник.
- **Гибкое разрешение плейсхолдеров:** добавлен параметр `-AllowPlaceholderTitles`. Также если список получен из явно заданного источника (`-SourceUrl` / выбранный HTML/URL в диалоге), названия вида `Эпизод N/Серия N` считаются допустимыми и применяются как есть.
- **UI/диалоги (WinForms):** введён единый минималистичный стиль окон и кнопок (нейтральный фон, единый шрифт, primary/secondary кнопки, аккуратный read-only блок текста), применён к `Show-WikipediaSearchForm`, `Show-EpisodeSourceUrlForm`, `Show-BilingualDialog`, `Show-RenameOutcomeDialog`.
- **Чистка кода:** удалена неиспользуемая функция `Show-InputBox`, добавлены общие helper-функции оформления (`Set-AppleDialogStyle`, `Set-AppleReadOnlyTextStyle`, `Set-AppleButtonStyle`) для снижения дублирования UI-кода.
- **Версионирование:** схема **0.M.P** (например **0.1.0** … **0.1.9**, затем **0.2.0** …); текущий сценарий — `Script_Rename_ALLVideo_0.2.8.ps1`; снимки в `Old/` переименованы под ту же схему; `Publish-ToolkitRelease.ps1` и `Rotate-ToolkitBackups.ps1` используют папку `Old`.

## 0.2.7 — 2026-04-20 23:17:17

- **Fetch-VideoMetadata.ps1:** добавлены `**Test-EpisodeTitleLooksLikePlaceholder`** и `**Test-EpisodeListLooksLikePlaceholderEpisodeTitles**`. Ранее список из CSV вида «Эпизод N» / «Серия N» (кириллица, но не названия) считался «полным» русским списком и **не** обогащался ru.wikipedia; при слиянии `**Merge-EpisodeTitlesPreferRu`** плейсхолдеры с кириллицей перетирали реальные названия из Википедии. Теперь при доле плейсхолдеров **> 35%** вызывается поиск по Википедии; `**Merge-EpisodeTitlesPreferRu`** подставляет wiki-строку, если локальная — плейсхолдер.
- **Script_Rename_ALLVideo_0.2.7.ps1:** при объединении с кандидатом из wiki локальная строка не имеет приоритета, если это плейсхолдер «Эпизод N» (и при этом есть кандидат с кириллицей). Снимок предыдущего релиза: `Old/Script_Rename_ALLVideo_0.2.6.ps1`.
- `**Переименовать-сериал.cmd`** вызывает `Script_Rename_ALLVideo_0.2.7.ps1`.

## 0.2.6 — 2026-04-20 23:05:48

- **Fetch-VideoMetadata.ps1 — `Get-EpisodesFromWikipediaHtmlFragment`:** разбор таблиц `**wikitable`** с `vevent` и ячейкой `**class="summary"**`, если нет класса `wikiepisodetable` (шаблон «Список серий», пример — «Заговор сестёр Гарви»). Раньше такие таблицы не учитывались, срабатывал запасной разбор по одним `<td>` и терялась последняя серия; в CSV оставалось только 9 из 10 названий.
- `**Get-EpisodesFromKinopoiskSavedHtmlFile`:** результат разбора сохранённой Википедии возвращается только если `**Test-EpisodeListSeasonEpisodeLooksSane`** — иначе не подставляются ложные 1–2 строки и дальше может сработать нормальный поиск списка эпизодов.

## 0.2.5 — 2026-04-20 13:08:45

- **Script_Rename_ALLVideo_0.2.5.ps1:** защита от регрессии на кейсе «Бриджертоны»: если wiki-обогащение списка эпизодов «роняет» сезон, присутствующий в локальном `episode-titles.csv` (пример: S03 пропадает из-за появления блока S04 на Wikipedia), локальный CSV сохраняется как источник истины.
- При обогащении введён приоритет локальных строк по ключу `season+episode`: если в локальном CSV уже есть кириллическое название, оно не перетирается; wiki-данные используются для дополнения пропусков и латинских/пустых значений.

## 0.2.4 — 2026-04-19 13:08:33

- **Fetch-VideoMetadata.ps1:** для таблиц `wikiepisodetable` сезон берётся и из заголовков **«N сезон: …»** (как у списка «Друзей» на ru.wikipedia), не только **«Сезон N»**; из ячейки названия — русская часть после типографского **» «** (англ. + рус. в одной ячейке); для **12—13** и т.п. в номере эпизода используется **первое** число (исключена склейка **1213**). В запасном разборе старых таблиц — первое число из ячеек сезона/эпизода.
- **Script_Rename_ALLVideo_0.2.4.ps1:** `**Normalize-EpisodeTitleForFileName`** — то же правило **» «** для строк в `episode-titles.csv`, если список не пересобирали.

## 0.2.3 — 2026-04-19 12:52:05

- **Скрипт:** `Script_Rename_ALLVideo_0.2.3.ps1`; снимок предыдущего релиза: `Old/Script_Rename_ALLVideo_0.2.2.ps1`.
- `Переименовать-сериал.cmd` вызывает `Script_Rename_ALLVideo_0.2.3.ps1`.
- `**Normalize-EpisodeTitleForFileName`:** строки вида `**Русское |AltTitle = English`** (шаблон Википедии в `episode-titles.csv`) больше не сводятся к английскому хвосту после `AltTitle`; выбирается сегмент с **кириллицей** (и обратный случай для TVDB: англ. + `AltTitle` с русским). Обрезка хвоста `**|RTitle =`**. Раньше «Землевладелец |AltTitle = Landman» превращалась в пустое имя после фильтра латиницы → в логе `**Лэндмен - S01E01.mkv`** без названия эпизода.

## 0.2.2 — 2026-04-19 12:33:28

- **Скрипт:** `Script_Rename_ALLVideo_0.2.2.ps1`; снимок предыдущего релиза: `Old/Script_Rename_ALLVideo_0.2.1.ps1`.
- `Переименовать-сериал.cmd` вызывает `Script_Rename_ALLVideo_0.2.2.ps1`.
- **Имена видеофайлов:** поддержка релизного шаблона `**.S1.E01.`** (точка между номером сезона и буквой `E`), в дополнение к `**S01E01`** / `S01E01-E02`. Раньше в лог попадало `DIAG: no SxxEyy filenames`, если в имени был только такой тег. Функция `**Get-EpisodeNumbersFromVideoFilename`**; обновлены `**Get-FallbackEpisodeTitleFromFilename`** и проверка в `**Write-RenameDiagnosticsIfNothingDone**`.

## 0.2.1 — 2026-04-19 07:07:36

- **Скрипт:** `Script_Rename_ALLVideo_0.2.1.ps1`; снимок предыдущего релиза: `Old/Script_Rename_ALLVideo_0.2.0.ps1`.
- `Переименовать-сериал.cmd` вызывает `Script_Rename_ALLVideo_0.2.1.ps1`.
- **Итоговый диалог** после `Done.`: вопрос об успешности, кнопки **Да / Нет / Назад / Отмена**, чекбокс **удалить** в папке сериала `episode-titles.csv`, `rename-series-log.txt`, `Переименовать-сериал.cmd` (**по умолчанию включён**). **Да** с чекбоксом — удаление (в `**-DryRun`** файлы не удаляются, выводится подсказка); **Да** без чекбокса — файлы остаются; **Нет** — в папке сериала создаётся ZIP `rename-series-debug-*.zip` (`debug-info.txt`, лог, CSV, при наличии `CURSOR-ASK-EPISODES.txt`) для передачи в Cursor; **Назад** — закрыть диалог без действий; **Отмена** — `exit 0`. Параметр `**-SkipOutcomeDialog`** отключает окно (автоматизация). Функции: `**Show-RenameOutcomeDialog`**, `**Export-RenameDebugBundle`**, `**Remove-RenameSeriesArtifacts**`.

## 0.2.0 — 2026-04-19 06:48:06

- **Скрипт:** `Script_Rename_ALLVideo_0.2.0.ps1`; снимок предыдущего релиза: `Old/Script_Rename_ALLVideo_0.1.9.ps1`.
- `Переименовать-сериал.cmd` вызывает `Script_Rename_ALLVideo_0.2.0.ps1`.
- **Папки «N сезон»:** переименование **файлов** выполняется и когда подпапка ещё называется «1 сезон» / «1-й сезон», а не только «Сезон 1» (раньше цикл смотрел только на шаблон «Сезон» + номер, поэтому в «1 сезон» видеофайлы не обрабатывались). Добавлена `**Resolve-SeasonNumberFromFolderName`**; расширен `**Get-NumericWordSeasonFolderRegex`** (вариант «N-й сезон», нормализация неразрывного пробела в имени папки). `**Write-RenameDiagnosticsIfNothingDone`:** папки «N сезон» считаются сезонными, чтобы не выводить ложное «нет подпапок Сезон N».
- `**Fetch-VideoMetadata.ps1`:** файл пересохранён в **UTF-8 с BOM**. Без BOM в Windows PowerShell 5.1 скрипт читался в системной кодировке, кириллица в строках ломалась («кракозябры»), парсер выдавал ложные ошибки на строках с регулярными выражениями.
- `**Get-EpisodesFromKinopoiskSavedHtmlFile`:** если файл — сохранённая **Википедия** (не Кинопоиск), после неудачи KP вызывается `**Get-EpisodesFromWikipediaHtmlFragment`** по содержимому файла. Раньше локальный `.htm` обрабатывался только парсером Кинопоиска → пустой список и сообщение «Наименований не найдено».

## 0.1.9 — 2026-04-19 06:40:43

- **Скрипт:** `Script_Rename_ALLVideo_0.1.9.ps1`; снимок предыдущего релиза: `Old/Script_Rename_ALLVideo_0.1.8.ps1`.
- `Переименовать-сериал.cmd` вызывает `Script_Rename_ALLVideo_0.1.9.ps1`.
- **Википедия (`Fetch-VideoMetadata.ps1`), `Get-EpisodesFromWikipediaHtmlFragment`:** в части таблиц `**wikiepisodetable`** только **две** колонки перед режиссёром: № эпизода в сезоне в `**th`** и название в `**td`** (без колонки «№ общий»). Раньше логика «три колонки» брала **номер эпизода из второй ячейки** и **название из третьей** — в итоге в название попадали **режиссёр** и т.п. (например «Аватар: Легенда об Аанге», 2024). Теперь: если первая ячейка — число, а вторая **не** число, используются **первая и вторая** колонки как номер в сезоне и название; если две ведущие ячейки — числа, по-прежнему **вторая и третья** (как у «Мандалорца»). Добавлена ветка `**elseif ($cells.Count -ge 2)`** для коротких строк.
- `**Format-RussianEpisodeTitleFromWikipedia`:** снятие «второго ряда» **латинского** названия без слова **Chapter** (`» « Aang`, `«English»` в конце), чтобы в имени файла оставалось только русское название (напр. **Аанг**).

## 0.1.8 — 2026-04-19 06:34:14

- **Скрипт:** `Script_Rename_ALLVideo_0.1.8.ps1`; снимок предыдущего релиза: `Old/Script_Rename_ALLVideo_0.1.7.ps1`.
- `Переименовать-сериал.cmd` вызывает `Script_Rename_ALLVideo_0.1.8.ps1`.
- **Папки сезонов в корне сериала:** дополнительно к шаблонам `**.S01.`**, `**( S01 )`**, релизному `**… S01 …**` подпапки вида `**1 сезон**`, `**2 сезон**` (число + пробел + слово «сезон», регистр не важен) переименовываются в `**Сезон 1**`, `**Сезон 2**`, … Функция `**Get-NumericWordSeasonFolderRegex**`, тот же целевой формат `**Get-PrefixSeasonFolder**`, что и для остальных шаблонов. Обновлены тексты диагностики `**Write-RenameDiagnosticsIfNothingDone**` (подсказка при конфликте имён для «N сезон»).

## 0.1.7 — 2026-04-19 06:29:21

- **Скрипт:** `Script_Rename_ALLVideo_0.1.7.ps1`; снимок предыдущего релиза: `Old/Script_Rename_ALLVideo_0.1.6.ps1`.
- `Переименовать-сериал.cmd` вызывает `Script_Rename_ALLVideo_0.1.7.ps1`.
- **Википедия (`Fetch-VideoMetadata.ps1`):** в колонке «Название» у части сезонов (в т. ч. **3-й** у «Мандалорца» в актуальной вёрстке) после русского заголовка стоит сноска `**<sup>… [англ.] …</sup>`** (interwiki на en.wikipedia). После удаления тегов в конце названия оставалась приписка `**[англ.]`** — она попадала в `**episode-titles.csv`** и в имя файла. Исправление: в `**Normalize-WikiCellText`** перед общим снятием тегов удаляются блоки `**<sup>…</sup>**`; в `**Format-RussianEpisodeTitleFromWikipedia**` дополнительно срезается текст `**[англ.]**` (на случай уже «плоского» текста). Русское имя берётся как в статье, без этой служебной пометки (ср. сохранённый HTML статьи).

## 0.1.6 — 2026-04-19 06:24:56

- **Скрипт:** `Script_Rename_ALLVideo_0.1.6.ps1`; снимок предыдущего релиза: `Old/Script_Rename_ALLVideo_0.1.5.ps1`.
- `Переименовать-сериал.cmd` вызывает `Script_Rename_ALLVideo_0.1.6.ps1`.
- **Версия набора:** `$script:ToolkitVersion = '0.1.6'` в основном сценарии.
- **Википедия (`Fetch-VideoMetadata.ps1`):**
  - Таблицы `**wikiepisodetable`**: сбор ячеек `**th`** и `**td**` в порядке следования в строке (раньше учитывались только `**td**` — сдвиг колонок: в «название» попадали режиссёр, дата премьеры, посторонний текст из других таблиц, в т.ч. слова вроде «Номинация»).
  - Строки эпизодов: класс `**vevent**` проверяется по **полному** фрагменту `<tr>…</tr>` (класс задаётся в открывающем теге `<tr>`; проверка только «внутренности» строки раньше отбрасывала все эпизоды).
  - **Сезон** эпизода — по последнему заголовку `**h3`** с «Сезон N» / «Season N» перед соответствующей таблицей.
  - `**Normalize-WikiCellText`:** вызов `**[System.Net.WebUtility]::HtmlDecode`** для сущностей вроде `«` / `»`, чтобы корректно снимались префиксы «Глава N:» и английский дубль.
  - `**Format-RussianEpisodeTitleFromWikipedia`:** снятие «Глава N:», хвоста `**» «Chapter …»`**, остатков англ. «Chapter …»; финальная очистка краевых `**»` / `«`**.
  - `**Get-EpisodesFromWikipediaParseHtmlApi`:** устойчивый разбор `**parse.text`** из JSON MediaWiki (в т.ч. свойство с ключом `*`), при наличии `**error`** в ответе — выход без HTML.
- Остальное поведение набора совпадает с **0.1.5** (папки сезонов и `SeasonFolderMatchRegex`, UNC и нормализация путей, `episode-titles.csv`, `AltTitle`, UTF-8 BOM основного сценария, правка `Publish-ToolkitRelease.ps1` для заголовка журнала).

## 0.1.5 — 2026-04-19 05:27:50

- **Папки сезонов:** параметр `**SeasonFolderMatchRegex`** по умолчанию распознаёт `**.S01.`**, `**( S01 )`**, а также релизное имя вида `**… S01 …**` (пробелы вокруг тега сезона, напр. `Supernatural S01 BDRemux …`). Номер сезона — именованная группа `**(?<sn>…)**` (или первая группа `()` в своём regex). Обновлены тексты диагностики и строка `SKIP` в `rename-series-log.txt`.
- **Скрипт:** `Script_Rename_ALLVideo_0.1.5.ps1`; снимок предыдущего релиза: `Old/Script_Rename_ALLVideo_0.1.4.ps1`.
- `Переименовать-сериал.cmd` вызывает `Script_Rename_ALLVideo_0.1.5.ps1`.
- **Кодировка:** основной сценарий сохраняется в **UTF-8 с BOM**, чтобы PowerShell 5.1 в Windows корректно разбирал кириллицу и строки с «умными» кавычками в диалогах.
- **UNC / сетевые папки:** корень сериала берётся через `Get-Item(...).FullName` и **нормализация пути** (снятие префикса `Microsoft.PowerShell.Core\FileSystem::`); экспорт `episode-titles.csv` через `**Set-Content -Encoding utf8`** вместо `[System.IO.File]::WriteAllLines`, чтобы запись на `\\сервер\...` не падала с «данный формат пути не поддерживается».
- **Имена файлов / русский заголовок:** если в ячейке `title` формат `**… |AltTitle = Русское название`** (часто у списков TVDB), в переименовании используется **только русская часть** после `AltTitle =`, без английского префикса.
- **Википедия (HTML):** задел под разбор `**wikiepisodetable`**; доработки парсера списка эпизодов — в **0.1.6**.
- **Служебно:** в `Publish-ToolkitRelease.ps1` разделитель в заголовке записи журнала — ASCII `-` (совместимость с PowerShell без BOM при запуске).

## 0.1.4 — 2026-04-19

- **CSV / кодировка:** `episode-titles.csv` сохраняется в **UTF-8 с BOM** (Excel и блокнот корректно определяют кириллицу); импорт читает **UTF-8 с/без BOM** и при необходимости **Windows-1251** (старые CSV из Excel). `Save-EpisodeListToCsv` в `Fetch-VideoMetadata.ps1` — тоже UTF-8 с BOM.
- **Скрипт:** `Script_Rename_ALLVideo_0.1.4.ps1`; снимок предыдущего релиза: `Old/Script_Rename_ALLVideo_0.1.3.ps1`.
- `Переименовать-сериал.cmd` вызывает `Script_Rename_ALLVideo_0.1.4.ps1`.
- **Правило Cursor (корень workspace):** `.cursor/rules/rename-video-toolkit-release.mdc` — после доработок набора журнал, версия, копия в `Old`.
- **Диагностика:** если нет подпапок `Сезон N` или в них нет файлов с `S01E01` в имени — перед `Done.` выводится пояснение (и строка в `rename-series-log.txt`), а не «тихий» выход.
- **Папки и файлы:** каталоги с шаблоном `**.S01.`** в имени переименуются в **«Сезон N»**; если каталоги **уже** называются **«Сезон N»**, шаг переименования каталогов не требуется — в лог пишется `SKIP folder rename: already «Сезон N»`. Затем внутри **«Сезон N»** переименовываются файлы с тегом **S01E01** в имени.

## 0.1.3 — 2026-04-19

- **Википедия (`Fetch-VideoMetadata.ps1`):** в `episode-titles.csv` больше не попадают **даты премьер** вместо названий эпизодов; отсев ложных таблиц по **номерам сезона/серии** и по правдоподобности ячейки «название»; короткий результат с **обычной статьи** о сериале (не страница списка) не принимается — ищется список эпизодов или переход по ссылкам; из длинного названия папки берётся **хвост после « - »** для запросов к Википедии; при структуре ru.wikipedia «**Название (N-й сезон)**» — **обход сезонных подстраниц** и сборка списка; при вводе URL статьи — тот же обход по сезонам, если основная страница не дала валидный список.
- **Скрипт:** `Script_Rename_ALLVideo_0.1.3.ps1`; предыдущий релиз: `Old/Script_Rename_ALLVideo_0.1.2.ps1`.
- `Переименовать-сериал.cmd` вызывает `Script_Rename_ALLVideo_0.1.3.ps1`.

## 0.1.2 — 2026-04-19

- **Русские названия:** при коротком/битом `episode-titles.csv` (в т.ч. только даты/финалы) список дополняется или заменяется данными **русской Википедии** (`Expand-EpisodeListWithRussianWikipedia`); то же после автозагрузки эпизодов перед сохранением CSV.
- **Кинопоиск (legacy):** разбор по строкам `<tr>`, заголовок эпизода только из `font-size:16px`; строки, похожие на **дату выхода**, в название не попадают.
- **Имена файлов:** по умолчанию **без** английского хвоста из имени релиза; пустое название → **«Серия N»**; опционально `-AllowEnglishFilenameFallback`.
- **Выпуск версий:** `Publish-ToolkitRelease.ps1 -NextVersion 0.1.5` — копия текущего скрипта в `Old`, новый файл в корне, обновление `.cmd`, запись в этот журнал, вызов `Rotate-ToolkitBackups.ps1`.
- Текущий релиз: `Script_Rename_ALLVideo_0.1.2.ps1` (заменён релизом 0.1.3); предыдущий снимок: `Old/Script_Rename_ALLVideo_0.1.1.ps1`.

## 0.1.1 — 2026-04-19

- Переименование основного сценария: `Script_Rename_ALLVideo_0.1.1.ps1` (релиз в корне набора). Копия предыдущего релиза: `Old/Script_Rename_ALLVideo_0.1.0.ps1`.
- **Кинопоиск (legacy HTML):** заголовки сезонов с `font-size:21px`, подписи эпизодов `Эпизод|Серия`, теги `<b>`/`<strong>`; fallback на прежний regex, если не найдено.
- **CSV:** импорт без ошибки из-за BOM UTF-8 в первой колонке; экспорт `episode-titles.csv` в UTF-8 **без BOM**.
- **Имена файлов:** поддержка `S09E23-24`; при отсутствии названия в таблице — **резервное имя** из части релиза после `SxxEyy` (англ. хвост как в имени файла).
- `Переименовать-сериал.cmd` вызывает `Script_Rename_ALLVideo_0.1.1.ps1`.
- Вспомогательный скрипт ротации резервных копий: `Rotate-ToolkitBackups.ps1` (порог 1000 файлов в `Old`, лишнее переносится в `archive`).

## 0.1.0 — начальный снимок

- Соответствует прежнему `Rename-Series.ps1` до версионирования (сохранён как `Old/Script_Rename_ALLVideo_0.1.0.ps1`).
