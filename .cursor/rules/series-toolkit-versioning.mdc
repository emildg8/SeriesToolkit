---
description: Версионирование SeriesToolkit и порядок CHANGELOG при работе в этом репозитории
alwaysApply: true
---

# SeriesToolkit: версии и документация

- **Стабильная линия SeriesToolkit — 0.1.27.** Расширенный inbox-конвейер и версии **не** SeriesToolkit — в каталоге `MediaInboxToolkit/` и правиле `.cursor/rules/media-inbox-toolkit.mdc`.
- Каждое **новое обращение пользователя** в рамках проекта `SeriesToolkit` / нормализации библиотек трактуй как повод выпустить **новую версию** (инкремент `version.json` через `SeriesToolkit/Bump-Version.ps1` или согласованно с пользователем, если bump уже был в этой же сессии). Не поднимай номер SeriesToolkit «ради» изменений только в `MediaInboxToolkit/`.
- Запись в **`SeriesToolkit/CHANGELOG.md`** для этой версии добавляется **всегда сверху** (сразу под заголовком `# CHANGELOG`), с датой/временем и перечнем смысловых изменений — не только строка автобампа.
- При изменениях движка, затрагивающих общий модуль, актуализируй и родительский **`Fetch-VideoMetadata.ps1`**, если логика метаданных там же.
- Сохраняй политику **снимков в `OLD/`** при bump (уже делает `Bump-Version.ps1`).
- Публикация: публичный репозиторий `emildg8/SeriesToolkit` и при необходимости второй remote через `Sync-GitHub.ps1` (`-SecondaryRemoteName` / `-SecondaryRemoteUrl`).
